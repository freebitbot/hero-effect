{
	"name": "@double-agent/collect-http2-session",
	"disabled": false,

	"main": "index.js",
	"private": true,
	"license": "MIT",
	"description": "Collects the settings, pings and frames sent across by a browser http2 client",
	"dependencies": {
		"@double-agent/collect": "workspace:*",
		"@double-agent/config": "workspace:*"
	}
}
